<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la petición es exactamente “/casino” o “/casino/”, redirige a public/
    RewriteRule ^$ public/ [L]

    # Para cualquier otra ruta, haz que apunte dentro de public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
